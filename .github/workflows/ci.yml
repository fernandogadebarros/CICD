name: Main Workflow

on:
    pull_request:
        type: [opened, synchronize]

jobs:
    linter:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3

            - name: Run Linter
              run: yarn lint
